
[root@ds-asterisk05 ~]# groupadd -g 4084 ext.arekunkov && useradd -u 4084 -g ext.arekunkov -c "SD#284469. Rekunkov Aleksandr Vasilyevich" -m -G wheel -s /bin/bash ext.arekunkov

[root@centos00 20170322-1726]# pwgen -c -n -s -C 10 1
j3lYdpRsO5
[root@centos00 20170322-1726]#

[root@ds-asterisk05 ~]# passwd ext.arekunkov
Changing password for user ext.arekunkov.
New password:
Retype new password:
passwd: all authentication tokens updated successfully.
[root@ds-asterisk05 ~]#

[root@ds-asterisk05 ~]# chage -d 0 ext.arekunkov
[root@ds-asterisk05 ~]# sudo -l -U !$
sudo -l -U ext.arekunkov
Matching Defaults entries for ext.arekunkov on this host:
    !visiblepw, always_set_home, env_reset, env_keep="COLORS DISPLAY HOSTNAME HISTSIZE INPUTRC KDEDIR LS_COLORS", env_keep+="MAIL PS1 PS2 QTDIR USERNAME LANG LC_ADDRESS LC_CTYPE", env_keep+="LC_COLLATE
    LC_IDENTIFICATION LC_MEASUREMENT LC_MESSAGES", env_keep+="LC_MONETARY LC_NAME LC_NUMERIC LC_PAPER LC_TELEPHONE", env_keep+="LC_TIME LC_ALL LANGUAGE LINGUAS _XKB_CHARSET XAUTHORITY",
    secure_path=/sbin\:/bin\:/usr/sbin\:/usr/bin, secure_path=/usr/local/sbin\:/usr/local/bin\:/usr/sbin\:/usr/bin\:/sbin\:/bin

User ext.arekunkov may run the following commands on this host:
    (ALL) ALL
[root@ds-asterisk05 ~]#

[root@ds-asterisk05 ~]# id ext.arekunkov
uid=4084(ext.arekunkov) gid=4084(ext.arekunkov) groups=10(wheel),4084(ext.arekunkov)
[root@ds-asterisk05 ~]#

................................................................................

[root@vm-asterisk06t ~]# groupadd -g 4084 ext.arekunkov && useradd -u 4084 -g ext.arekunkov -c "SD#284469. Rekunkov Aleksandr Vasilyevich" -m -G wheel -s /bin/bash ext.arekunkov
[root@vm-asterisk06t ~]# passwd ext.arekunkov
Changing password for user ext.arekunkov.
New password:
Retype new password:
passwd: all authentication tokens updated successfully.
[root@vm-asterisk06t ~]# chage -d 0 ext.arekunkov
[root@vm-asterisk06t ~]# sudo -l -U !$
sudo -l -U ext.arekunkov
Matching Defaults entries for ext.arekunkov on this host:
    !visiblepw, always_set_home, env_reset, env_keep="COLORS DISPLAY HOSTNAME HISTSIZE INPUTRC KDEDIR LS_COLORS", env_keep+="MAIL PS1 PS2 QTDIR USERNAME LANG LC_ADDRESS LC_CTYPE", env_keep+="LC_COLLATE
    LC_IDENTIFICATION LC_MEASUREMENT LC_MESSAGES", env_keep+="LC_MONETARY LC_NAME LC_NUMERIC LC_PAPER LC_TELEPHONE", env_keep+="LC_TIME LC_ALL LANGUAGE LINGUAS _XKB_CHARSET XAUTHORITY",
    secure_path=/sbin\:/bin\:/usr/sbin\:/usr/bin, secure_path=/usr/local/sbin\:/usr/local/bin\:/usr/sbin\:/usr/bin\:/sbin\:/bin

User ext.arekunkov may run the following commands on this host:
    (ALL) ALL
[root@vm-asterisk06t ~]# id ext.arekunkov
uid=4084(ext.arekunkov) gid=4084(ext.arekunkov) groups=10(wheel),4084(ext.arekunkov)
[root@vm-asterisk06t ~]#


